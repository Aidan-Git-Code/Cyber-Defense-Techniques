# Ansible inventory for vulnerable Docker deployment.
# Use localhost for same-machine testing, or replace with remote host.

[docker_hosts]
#localhost ansible_connection=local

# Remote example (uncomment and set your target):
target ansible_host=100.65.3.196 ansible_user=cyberrange

# [docker_hosts:vars]
# ansible_user=ubuntu
# ansible_ssh_private_key_file=~/.ssh/id_rsa
